{
  "name": "aerocode",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "migrate": "backend/node_modules/.bin/prisma.cmd migrate dev --schema=prisma/schema.prisma",
    "seed": "backend/node_modules/.bin/ts-node.cmd prisma/seed.ts",
    "setup": "backend/node_modules/.bin/prisma.cmd migrate dev --name init --schema=prisma/schema.prisma && backend/node_modules/.bin/ts-node.cmd prisma/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}
